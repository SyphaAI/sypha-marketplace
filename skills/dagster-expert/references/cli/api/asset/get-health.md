---
title: dg api asset get-health
triggers:
  - "getting asset health or runtime status"
---

Retrieve health and runtime status details for an asset.

```bash
dg api asset get-health <ASSET_KEY>
```

When the asset key includes a prefix, provide it using slash-separated syntax: `dg api asset get-health my_prefix/my_asset`.
