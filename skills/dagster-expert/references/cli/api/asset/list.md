---
title: dg api asset list
triggers:
  - "querying which assets exist in a deployment"
---

```bash
dg api asset list
```

- `--status` — adds detailed status information to the response
- `--limit` / `--cursor` — controls result pagination
