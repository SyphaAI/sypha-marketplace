---
title: dg api deployment settings-set
triggers:
  - "setting deployment settings in Dagster Plus"
---

Applies deployment settings from a YAML file.

```bash
dg api deployment settings-set <FILE>
```

- `<FILE>` — path to a YAML file that defines the desired deployment settings
