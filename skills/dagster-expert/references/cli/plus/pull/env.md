---
title: dg plus pull env
triggers:
  - "pulling environment variables from Dagster Plus into a local .env file"
---

Fetch environment variables from Dagster Plus and write them to a local `.env` file. Authentication via `dg plus login` is required before running this command.

```bash
dg plus pull env
```
